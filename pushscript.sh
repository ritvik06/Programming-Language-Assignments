#!/bin/sh
git add .
git config user.name 'ritvik06'
git commit -m 'one updated file'
git push origin master

#expand this script for prompts, add certain files or the default add ., add prompt for commiting the message, add prompt for setting the remote, taking details input for 
#refs etc.

#make automated script for the carla environment for the model to train and then the model to load in the client_example.py and then give the controls to the server,give prompts for pull, use force merge in cases of prompt(have to look into ways for merging conflits or more like removing them)

#testing
